<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:statusBarColor" tools:targetApi="21">@color/colorPrimaryDark</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="21">true</item>

        <item name="android:alertDialogTheme">@style/AppTheme.AlertDialog</item>
    </style>

    <style name="AppTheme.AlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTitleStyle">@style/AppTheme.AlertDialog.Title</item>
    </style>

    <style name="AppTheme.AlertDialog.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AlertDialog.Title.Text" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Buttons -->

    <style name="AppTheme.Button.Present" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPresent</item>
    </style>

    <style name="AppTheme.Button.Absent" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorAbsent</item>
    </style>

    <style name="AppTheme.Button.Leaving" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorLeaving</item>
    </style>

</resources>
